import { Controller, Get, Param, Post, UseGuards } from '@nestjs/common';import { AppointmentService } from './appointment.service';import { JwtAuthGuard } from '../auth/jwt/jwt-auth.guard';import { RolesGuard } from '../auth/jwt/roles.guard';import { Role } from '../auth/entities/account.dto';import { Roles } from '../auth/jwt/roles.decorate';@Controller('appointments')export class AppointmentController {  constructor(private readonly appointmentService: AppointmentService) {}  @UseGuards(JwtAuthGuard, RolesGuard)  @Roles(Role.DOCTOR, Role.MANAGER)  @Post('autoCreate')  async createQueue() {    return this.appointmentService.autoCreateQueue();  }  @UseGuards(JwtAuthGuard, RolesGuard)  @Roles(Role.PATIENT)  @Get('findAll/:id')  async findAllById(@Param('id') patientId: string) {    return this.appointmentService.findAllById(patientId);  }}