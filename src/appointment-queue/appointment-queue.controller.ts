import { Body, Controller, Post, UseGuards } from '@nestjs/common';import { AppointmentQueueService } from './appointment-queue.service';import { JwtAuthGuard } from '../auth/jwt/jwt-auth.guard';import { RolesGuard } from '../auth/jwt/roles.guard';import { Role } from '../auth/entities/account.dto';import { Roles } from '../auth/jwt/roles.decorate';@Controller('appointment-queues')export class AppointmentQueueController {  constructor(    private readonly appointmentQueueService: AppointmentQueueService,  ) {}  @UseGuards(JwtAuthGuard, RolesGuard)  @Roles(Role.DOCTOR, Role.MANAGER)  @Post('autoCreate')  async createQueue() {    return this.appointmentQueueService.autoCreateQueue();  }}